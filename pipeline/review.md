# Review

**Verdict: done** — mergeable as-is.

## Summary
`CONTRIBUTING.md` was added at the repo root and fully satisfies the brief:
it states that PRs are opened by an AI software factory, merged by humans,
and instructs that new work be filed as GitHub issues. Content matches the
plan closely (goal, approach, and file list all followed).

## Findings
None blocking.

Minor/optional (not must-fix):
- No heading anchor or table of contents, but the file is short (10 lines)
  so this is unnecessary.
- The file doesn't mention licensing/CLA or code style, but the brief only
  asked for a short note about the PR/issue workflow, so this is correctly
  scoped and not an omission.

## Diff hygiene
Only `CONTRIBUTING.md` plus the expected `pipeline/brief.md` and
`pipeline/plan.md` artifacts were added. No unrelated or unintended
changes.
